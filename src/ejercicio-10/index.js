// Ejercicio 10 - Convertir a Cadena

// Dado un número, utiliza el método toString() para convertirlo en una cadena de texto. Luego, imprime el resultado.
const numero = 42
const cadenaNumero = numero.toString()

console.log(cadenaNumero)
// Resultado: '42'
